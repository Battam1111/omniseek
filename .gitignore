# Python
__pycache__/
dist/
build/
*.py[cod]
*.egg-info/
.ruff_cache/
.pytest_cache/
.venv/

# OS / editor
.DS_Store
Thumbs.db

# Never commit credentials or local state (they live in ~/.omniseek on the host)
*credentials*.json
*.token
*.secret

*.stackdump

# Local runtime state mounted by docker-compose
.omniseek/
.penumbra/

# Benchmark runtime artifacts
bench/results/
bench/RESULTS.md

# build intermediates of the asset masters; the canonical renders live in assets/
assets/masters/*.html
assets/masters/*.png
